apiVersion: v1
kind: Service
metadata:
  name: rish-sample-node-app
spec:
  type: NodePort
  selector:
    app: rish-sample-node-app
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080
